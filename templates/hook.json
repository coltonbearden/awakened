{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "timeout": 10,
            "prompt": "Before the turn ends, check the hook input for evidence that every verification the user asked for was actually run and its result reported. If a requested check was skipped or its outcome was asserted rather than shown, respond with {\"ok\": false, \"reason\": \"<the specific check still to run or report>\"}; otherwise respond with {\"ok\": true}. Do not execute commands, write files, reach the network, install dependencies, or request credentials as part of this hook. Hook input: $ARGUMENTS"
          }
        ]
      }
    ]
  }
}
